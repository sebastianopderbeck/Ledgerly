import { motion } from "framer-motion";
import { Box, List, ListItem, TableBody, TableRow } from "@mui/material";

export const MotionBox = motion.create(Box);
export const MotionList = motion.create(List);
export const MotionListItem = motion.create(ListItem);
export const MotionTableBody = motion.create(TableBody);
export const MotionTableRow = motion.create(TableRow);
